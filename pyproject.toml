[project]
name = "red-teaming-probes"
version = "0.1.0"
description = "Red teaming probes project"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "trl",
    "datasets",
    "transformers",
    "peft",
    "wandb",
]


